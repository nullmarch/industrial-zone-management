<Page x:Class="WpfApplication2.Views.Login.Connexion"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      
      xmlns:local="clr-namespace:WpfApplication2.Views.Login"
      mc:Ignorable="d" 
      d:DesignHeight="760" d:DesignWidth="450"
      Title="Connexion">



    <Grid Background="White">


            

                <TextBlock Margin="0 100 0 0" VerticalAlignment="Top" HorizontalAlignment="Center" FontSize="28"
                           FontWeight="Bold" Text="Bienvenue!"/>
                <TextBlock FontSize="17" FontWeight="SemiBold" Margin="0 140 0 0" VerticalAlignment="Top" HorizontalAlignment="Center" Text="se connecter "/>

                <TextBox Margin="0 0 0 80" x:Name="UsernameInput" Width="300 " VerticalAlignment="Center" FontSize="18" materialDesign:HintAssist.Hint="saisir le nom d'utilisateur"
                         BorderThickness="2" BorderBrush="{DynamicResource MaterialDesignDivider}" />
                <PasswordBox  Margin="0 100 0 80" x:Name="PasswordInput" Width="300" FontSize="18" 
                             materialDesign:HintAssist.Hint="Saisir le mot de passe" VerticalAlignment="Center" BorderThickness="2" BorderBrush="{DynamicResource MaterialDesignDivider}"
           />
                <Button  x:Name="BtnConnx" Click="BtnConnx_Click" Margin="0 200 0 60" VerticalAlignment="Center"  Style="{StaticResource MaterialDesignFlatButton}" materialDesign:ShadowAssist.ShadowDepth="Depth0" 
                        Height="53" Width="300" 
                FontSize="18" Content="Connexion"/>

                <Button  x:Name="BtnCreeU" Click="BtnCreeU_Click" Margin="0 320 0 60" VerticalAlignment="Center"  Style="{StaticResource MaterialDesignFlatButton}" materialDesign:ShadowAssist.ShadowDepth="Depth0" 
                        Height="53" Width="300" 
                FontSize="18" Content="Créer Utilisateur"/>


              


        







      

    </Grid>
</Page>
